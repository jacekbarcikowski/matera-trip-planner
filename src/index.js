import React from 'react';
import ReactDOM from 'react-dom/client';
import MateraTripPlanner from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MateraTripPlanner />
  </React.StrictMode>
);
